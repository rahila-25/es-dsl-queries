FROM logs-*
| WHERE cls.application == "epp-settlement-batch-filetransfer"
  AND cls.environment LIKE "prod%"
  AND level == "ERROR"
  AND message LIKE "*Missed_Business_SLA*"
| KEEP @timestamp, cls.application, cls.environment, level, message
| SORT @timestamp DESC
| LIMIT 100
