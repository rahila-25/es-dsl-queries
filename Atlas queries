FROM logs-*
| WHERE cls.application == "epp-settlement-batch-filetransfer"
  AND cls.environment LIKE "prod%"
  AND level == "ERROR"
  AND message LIKE "*Missed Business SLA*"
  AND @timestamp >= NOW() - 30 DAYS
| STATS impact_count = COUNT(message)
